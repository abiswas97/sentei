# Tasks: star-shimmer-vocabulary

## 1. Implementation

- [x] 1.1 Motion clock: `motionTickMsg` chain gated by `motionActive`, pure `starFrame`/`starColor`/`shimmerLine` in motion.go, ramp tokens in both palettes, bubbles/spinner removed; tests (TDD)
- [x] 1.2 Vocabulary: `✦` done + verdict, `✓` deleted, `▸` would-act preview lines, `✻` fallback, `●` swept with an invariant test across major views
- [x] 1.3 Render sites: phase headline + step rows shimmer via injected Motion presentation, stat line star, scan/running/menu-hint shimmer
- [x] 1.4 Docs: `.impeccable.md` vocabulary table, timing, decision log
- [x] 1.5 Gauntlet; VHS GIF verification (playground wide + slow-deletion repo) with frame review; PR, CI, merge
