# Design: golden-views

Deterministic fixtures only: fixed width/height, settled (non-working)
states so the motion clock never appears, and commit dates as short
now-relative offsets so age buckets are stable at any test time. Working
views (progress, scans) stay out — they are the VHS loop's job.
