## Design Context

### Users
Developer power users managing multiple git worktrees in bare repository setups. They use the terminal daily, expect keyboard-driven workflows, and value tools that are fast, safe, and informative. The primary job: manage the full worktree lifecycle (create, setup, remove, cleanup) without memorizing scripts or flags.

### Brand Personality
Friendly, polished, capable. Like a well-made tool that feels good to use — not flashy, but clearly crafted with care. Think the Charm ecosystem (gh, gum, soft-serve) rather than raw ncurses.

### Aesthetic Direction
- **Tone:** Approachable and polished. Good use of whitespace, clear hierarchy, thoughtful color.
- **References:** gh CLI, lazygit (information density), Charm tools (visual polish).
- **Anti-references:** Dense walls of unformatted text. Overly colorful / rainbow terminal output.
- **Theme:** Adaptive — `tea.RequestBackgroundColor` at startup selects the dark or light palette; dark is the default when the terminal does not answer.
- **Current palette:** Two palettes declared as data in `internal/tui/styles.go` (`darkPalette`/`lightPalette`); `applyPalette` is the single construction path for every token and style. Styles and overlays reference tokens, never raw color values. Rounded borders for dialogs.

| Token | Dark | Light | Role |
|-------|------|-------|------|
| `colorAccent` | 62 | 56 | purple: active, accent, borders |
| `colorSuccess` | 42 | 29 | green: clean, done, success |
| `colorWarning` | 214 | 166 | orange: dirty, warnings |
| `colorError` | 196 | 160 | red: untracked, failed, errors |
| `colorDim` | 241 | 245 | gray: secondary text, pending |
| `colorEmphasis` | 15 | 235 | titles, cursor row |
| `colorBody` | 252 | 238 | normal rows |
| `colorSelected` | 212 | 168 | pink: selected rows |
| `colorProtected` | 63 | 26 | blue: protected worktrees |
| `colorMuted` | 245 | 243 | gray: locked worktrees |

Light values preserve the dark theme's prominence ordering by inverting the lightness ladder (emphasis darkest, dim lightest).

### Decision Log
- **2026-06-11 Color tokens:** every color is a named token in `styles.go`; raw color values appear only in the palette block. (this change)
- **2026-06-11 Adaptive theme:** implemented via `tea.RequestBackgroundColor` + `BackgroundColorMsg.IsDark()` selecting between the two palette tables; dark by default. A `Theme` struct injected through views was evaluated and deferred: it touches every render signature for no behavioral gain until a third theme dimension (user themes) exists. (this change)
- **2026-06-11 Key hints:** adopt `bubbles/help` fed from `keys.go` bindings as the single source for hint footers and help content, replacing `viewKeyHints` and the hand-written strings in `help.go`.
- **2026-06-11 Spinner:** indeterminate waits (repository scan, menu loading) get `bubbles/spinner`; determinate progress keeps the bar.
- **2026-06-11 Worktree list:** stays on `lipgloss/table` with `StyleFunc`; `bubbles/list` was evaluated and declined because it fights the multi-select checkbox column, custom sort headers, and shared view chrome.
- **2026-06-11 Charm v2 platform:** the TUI builds on `charm.land/{bubbletea,bubbles,lipgloss}/v2`. Terminal features (alt screen, mouse mode) are declared on the root `tea.View` only; the v2 renderer always requests kitty key disambiguation, which makes `ctrl+enter` quick-create real on supporting terminals. The adaptive-theme implementation path is now `tea.RequestBackgroundColor` + `BackgroundColorMsg.IsDark()` rather than v1 `AdaptiveColor`. (this change)
- **2026-06-11 Virtual cursor:** text inputs keep the bubbles virtual cursor (v2.1 default) instead of the real terminal cursor; real-cursor adoption is deferred to the huh-forms evaluation. (this change)
- **2026-06-11 Mouse wheel:** the wheel is an input alias, never new behavior: list and integration cursors move as with j/k, the info carousel pages as with h/l, the portal viewport scrolls. Click and motion are not handled. (this change)
- **2026-06-11 bubbles/help adopted:** footers render via `bubbles/help` from per-view binding declarations in `keys.go` (`viewFooter`/`footerHints` in chrome.go); `viewKeyHints`, the `KeyHint` type, and the hand-written tables in help.go are gone. The library gives up truncating when the boundary item leaves no ellipsis room, so `footerHints` enforces the budget itself. (this change)
- **2026-06-11 Spinner shipped:** indeterminate waits (cleanup scan, menu load) animate via a shared `bubbles/spinner` MiniDot wrapped in the accent style at render time; ticks are gated by `indeterminateWaitActive` and die when no wait is visible. Determinate progress keeps the static vocabulary. (this change)
- **2026-06-11 Portal absorbed the info card:** integration details moved from the bespoke carousel overlay (`styleInfoCard`, h/l paging) to the standard `?` DetailPortal as one scrollable page listing all integrations; the Left/Right bindings died with it. View body sizing was simultaneously hoisted into the global Update (`viewChromeRows` constant), deleting 23 identical per-view WindowSizeMsg handlers. Portal content is wrapped to the portal width and the viewport horizontal bindings are disabled: the portal scrolls vertically only. (this change)
- **2026-06-11 Animated progress + elapsed:** the overall bar springs toward targets via `bubbles/progress` (frames gated to live progress views, spring discarded between flows in `holdOrAdvance`); elapsed derives from `progressStartedAt` with `bubbles/stopwatch` as the once-a-second repaint ticker. Each flow's layout construction is a method shared by view and spring target. fang+cobra was evaluated and declined: the hand-rolled CLI registry is small and tested; revisit if the subcommand surface grows. (this change)
- **2026-06-11 Bar coherence fix:** the percentage label now follows the displayed fill (native component percentage) instead of stating the target, after the demo showed a partial bar labeled 100%; the removal flow syncs the spring at its prune/cleanup completion stages, and the spring is tuned (frequency 30) to settle within the hold. (this change)
- **2026-06-11 Progress endings:** flows may not transition until the layout tells the truth: completion sets `ProgressLayout.Completed`, no-work phases render dim `– skipped` and stop counting as outstanding, every flow issues a final spring sync at its completion message, quitting mid-progress leaves a stderr trace naming the operation, and the summary cd line wraps in full. Repo-flow upfront step plans deferred pending a pipeline plan-event type. (this change)
- **2026-06-11 Cleanup truth chain:** the aggressive headline, gate, and footer hint all derive from `DeletableAggressiveCount()` so the bold emphasis always states what WILL happen ("1 of 3", "none deletable without --force"); a confirm prompt for zero deletions cannot exist; the result never claims clean beside skips; the clean preview lists the five checks in the result's order; the command echo is labeled `ran:`. (this change)
- **2026-06-11 Canvas compositor:** `compositeOverlay` now composes lipgloss v2 `Canvas` + `Compositor` layers instead of hand-splicing ANSI; overlay rows are claimed in full (centered with cleared flanks) so no orphan background glyphs survive beside the portal box, while the background stays visible above and below. Note: `Layer.X/Y/Z` only take effect through `Compositor`; bare `Canvas.Compose(layer)` draws at origin. (this change)
- **2026-06-11 Tab presence:** the root view sets WindowTitle (`sentei · repo · removing 2/3`) and mirrors flow progress into the terminal's native OSC 9;4 indicator (indeterminate scans, error on failed phases) from the same overall() source as the spring; flowIdentity is the single table naming operations for both the quit trace and the tab verb. (this change)
- **2026-06-11 Selection and danger weight:** cursors are `▸` everywhere (menu, list, integrations) with the selected label in accent; `worktreeLabel` is the one naming rule (branch, else short hash, else `(prunable)`, else basename) used by list, confirm, and progress alike; the confirm screen speaks badge vocabulary (`[ok] clean`, `[~]`, `[!]`) instead of prose parentheses; destructive footers lead with the danger key in warning weight via `viewFooterDanger` (`y delete` orange, the rest dim); the delete hint only exists while something is selected (`listFooterNoSelection` at zero); integration rows reserve the pending-count line so toggling never shifts layout, and `[x]` is neutral in rows and legend because green is the *change* color (`[+]`), not the steady state.
- **2026-06-11 Progress motion:** the static `◐` retired for a breathing dot (`· ∙ ● ∙`, ~1.3s cycle): the pending dot inflating toward the done dot, so the animation deepens the existing vocabulary instead of replacing it (alternatives weighed: rotating `◐◓◑◒`, braille MiniDot, Crush arrow+ellipsis). Injected through `ProgressLayout.ActiveGlyph` like the bar; pure layouts fall back to static `∙`; the flow-entry tick starts in one place (the dispatch wrapper), not at the 12 entry sites. The overall bar fills the line minus a fixed elapsed reserve (floor 20) and blends `barStart→barEnd` (hex twins of accent and selected, per palette) scaled to the fill — harmonica was considered for the indicator and declined: its spring already animates the bar, and a frame spinner is the right tool for a one-cell glyph. (this change)
- **2026-06-11 Motion smoothing (revision after user GIF review):** the breathing dot was staccato — 4 frames at 3fps, and the smoothness ceiling is glyph resolution (Unicode has ~4 usable dot sizes), so rather than tune it the working indicator standardized on the heavy braille dot (`workFrames`, single-cell, 10fps; the bubbles builtin ships two-cell frames). One vocabulary collapsed two spinner instances into one: `m.spin` carries every working surface behind one gate (`spinnerActive`), and explicit tick starts were removed so no entry path double-starts the chain (parallel chains double the frame rate). The bar spring dropped from frequency 30 to 8: fills glide instead of snapping, still settling inside the hold. Transitions stay instant cuts by decision — motion belongs to state-driven elements, never keypress navigation; Canvas+harmonica slide transitions were considered and declined as latency on every nav. (this change)
- **2026-06-11 Motion polish (second revision, user GIF review):** braille retired after one day — its frames use the bottom braille dot row, which fonts place in the descender zone, so the spinner hung below the text baseline, sat flush-left, and weighed a third of the bold text beside it (font geometry, unfixable from the app). The working indicator is the breathing dot done properly: `· ∙ • ● • ∙` at 10fps (0.6s cycle — the original rejection was the 3fps staccato, not the concept), the only frame family optically centered and weight-matched because it *is* the `·`/`●` family. Verdict/state split: `✓` marks operation-level success on summary headlines (pairs with `✗`, charm-canon), `●` stays an item state among peers — a lone green disc on a headline read as unexplained ("why is this a circle?"). Bar spring 8→6 (~1.2s full sweep) plus `progressSettleFloor`: the hold was measured from view entry, so flows outliving it cut away mid-glide at ~94%; the floor gives every held flow a settled beat at 100%. Noted: cleanup preview uses `●` for would-act lines (future action, a third meaning) — acceptable against dim `·` no-ops, revisit if it confuses. (this change)
- **2026-06-12 Star + shimmer vocabulary:** after three motion rounds the user chose a new language from a live HTML lab (`/tmp/audit-tapes/star-lab.html`): the Claude Code star twinkle (`· ✢ ✳ ✻ ✽`, 120ms) plus Crush-style shimmer on all working text, each line in its own color family (V4 ensemble), with the full circle retired outright. `✦` carries done rows *and* the success verdict — the one-day-old `✓` verdict split dissolved because the star family explains itself: you watch the twinkle crystallize. Would-act preview lines moved to accent `▸`. All motion now derives from one deterministic 60ms clock (`motionTick`) as pure functions — star frame, star color, per-rune shimmer band — replacing bubbles/spinner; ramps are palette data with light-theme variants. Invariant test: `●`/`◐`/`✓` appear in no rendered view. (this change)
- **2026-06-12 Confirm columns:** real-run review found the confirm rows ragged (`name [badge] note` inline). Rows are now columnar per the layout lab (C1): badge in a fixed 4-cell gutter so one vertical sweep answers "anything risky?", names rune-padded to the longest label capped at `confirmNameWidthCap`, risk notes only on at-risk rows — clean rows drop the word "clean" because the green `[ok]` carries it. Mirrors the worktree list's status-first column. (this change)
- **2026-06-12 Integration robustness:** a real apply failure shredded the UI and exposed a chain: the default detect probe `<binary> --version` exits non-zero for CLIs without that flag (ccc), so an existing pipx install went undetected; the resulting `uv tool install` refused to overwrite pipx's executable; and the failure's error — carrying uv's entire output — rendered raw. Fixes: detection defaults to `command -v <binary>` (presence, not flag roulette; explicit Detect.Command still wins); failed steps render a bounded peek (first line dim, last non-empty line red — installers end with `error:` — then `… N more — ? for full output`), live progress rows clamp to the last line only, and the detail portal carries the untrimmed output, unlocking whenever a failure has output. (this change)
- **2026-06-12 Voice registry (Wave 2c):** all 28 view/portal titles live in `copy.go` as consts, sentence case ("Remove worktrees", not "Remove Worktrees") — changing the voice is a one-line edit; `?` says "details" everywhere (was info/details drift), menu select is "select"; portal boxes render their bare title because the brand is already on the chrome behind them. (this change)
- **2026-06-12 Light contrast (Wave 2d):** light palette dim 245→243 (~3.5:1 on white was below the 4.5:1 text floor) and warning 166→130 (washed-out orange to ~4.6:1, still recognizably orange); dark palette untouched; verified with a light-theme VHS tape — background detection picked the light palette and the at-risk badges read clearly on white. (this change)
- **2026-06-12 Completion settle (Wave 3a):** once a flow's result arrives, the bar's fill renders the success gradient (`barDoneStart→barDoneEnd`) for the hold — the bar joins the ✦ moment. Settle-to-green over the audit's flash-and-revert: a revert reads as a glitch. The integration flow gained the `Completed` flag it was missing (`integ.finalized`). (this change)
- **2026-06-12 Milestone whisper (Wave 3b):** the repo state carries `LifetimeRemoved`; when a removal run crosses a power of ten the summary whispers one dim line (`that was your 100th worktree, pruned`). Garnish degrades silently — state errors never surface here; the message is handled globally because the hold can advance the view before the recording lands. (this change)

### Visual Evolution
As sentei grows from a cleanup tool to a full worktree manager, the visual identity should evolve with scope. New views (creation flows, repo operations) can introduce richer patterns — step indicators, section headers, progress sequences — while maintaining the core color vocabulary and Charm-style polish.

### Design Principles
1. **Information hierarchy first** — Most important data (branch name, status, age) should be instantly scannable. Secondary details (commit subjects, paths) are available but don't compete.
2. **Color means something** — Every color carries semantic meaning (status, emphasis, warning). Never use color purely for decoration.
3. **Keyboard-native** — Every action is reachable via keyboard. Vim-style (j/k) plus standard keys (arrows, space, enter). Discoverable via status bar hints.
4. **Safe by default** — Destructive actions require confirmation. Warnings are visually distinct (yellow/red). Protected items are clearly marked and non-selectable.
5. **Progressive disclosure** — Show the essential view first, let users drill into details. Don't front-load every piece of information.

## Component Patterns

### View Chrome
Every full-screen view renders the same frame, via the pure helpers in `internal/tui/chrome.go`:
- **Title:** `viewTitle(titleX)` → `  sentei ─ View name`, bold white (15), sentence case, declared once in `copy.go`. No backgrounds, no badges.
- **Subtitle (optional):** one dim or accent context line under the title (repo name, branch arrow).
- **Separators:** `viewSeparator(width)` → dim (241) `┄` rule, 2-space pad, width-4 long. One above content, one below.
- **Key hints:** `viewFooter(width, <view>Footer)` → dim, joined with ` · `, rendered by `bubbles/help` from bindings declared in `keys.go`. Always the last line. Never use `•`. Rows that exceed the width drop trailing hints with a ` …` marker.

### Progress Views
All long-running flows render through `ProgressLayout` (`internal/tui/progress_layout.go`) fed `phaseDisplay` data:
- Phase header: `  <indicator> <Name>  <done>/<total>  <pct>%`, indicator left of the name.
- Steps indent 4 spaces under their phase; completed phases collapse to one line unless they contain failures.
- A phase with no discovered work (`total == 0`) is `pending`, never 100%.
- Overall bar: fills the content width minus a fixed elapsed reserve (floor 20 cells, `overallBarWidth`), gradient fill `barStart→barEnd` scaled to the filled portion, dim track, spring-animated toward each completion target (`bubbles/progress`, frequency 6: a full sweep takes ~1.2s); the percentage label follows the displayed fill so bar and label never disagree (phase headers state actual counts), settling at full within the completion hold. A dim `elapsed Ns` readout sits beside it. Clamped to 0-100.
- Large step lists window to terminal height: failed and active steps always visible, then `WindowCompletedTrail` recent completions and `WindowPendingLead` upcoming, with a stat line (`✦ N done · ✻ N active · · N pending  showing X of Y`).

### Status Indicators
| Char | Meaning | Color |
|------|---------|-------|
| `✦` | done / succeeded — item rows and verdict headlines | green 42 |
| `· ✢ ✳ ✻ ✽` | working (star twinkle, 120ms, inside its line's shimmer band) | accent 62 |
| `·` | pending — the star's resting frame | gray 241 |
| `✗` | failed | red 196 |
| `⚠` | warning | orange 214 |
| `▸` | would act (cleanup preview) | accent 62 |

One family tells the lifecycle: the pending dot twinkles into a star while
sentei works and crystallizes into `✦` when done; `✦` also leads success
headlines. `●` and `✓` are retired everywhere (invariant-tested). Every
working line carries a shimmer band sweeping its text in the text's own
color family (`rampAccent` on phase headlines, scans, and running lines;
`rampBody` on working step labels; `rampDim` on menu loading hints), with
the star inside the band. Counts and meta never shimmer. The core rule:
anything moving is being worked on; anything still is settled. Pure layouts
without injected motion fall back to a static `✻`.

### Text Overflow
Paths, branch names, and error text that can exceed the width go through `truncateWithEllipsis` — content never hard-clips at the terminal edge. List rows are always exactly one line: cells pre-truncate to their column widths (`Wrap(false)`), the Subject column drops below 72 columns and Age below 56, and the legend compacts to badges alone when the full form cannot fit.

### Timing
Progress views hold for `minProgressDuration` (`WithMinProgressDuration`, 1.5s in playground) via `holdOrAdvance` before advancing, so fast operations stay legible; when holds are enabled, `progressSettleFloor` guarantees at least 1s after the final event so the bar visibly finishes at 100% instead of cutting away mid-glide. The only other timing mechanism is the motion clock: a 60ms tick counter scoped to `motionActive` (any visible work: waits, progress views, the cleanup running line), started in exactly one place per entry path (Init, or the dispatch wrapper on transitions into a working state), from which star frames (120ms) and shimmer band positions (2.5s sweep) derive as pure functions. View-to-view navigation cuts instantly: motion belongs to state-driven elements (spring, spinner, holds), never to keypress navigation.

### Key Mapping
All bindings live in `internal/tui/keys.go` and are referenced, never redefined locally. Per-view presentation (footer subsets and named help sections) is also declared there: the same physical key may carry a different description per view (`enter` = delete / continue / confirm), declared once per view via `withDesc`, never at render sites. Footers and the F1 help portal derive from the same declarations, so they cannot disagree. `?` is contextual info for the current view; `F1` is reserved for global help (portal change). The worktree list footer is curated to fit 80 columns beside the selection count; select-all and sort live in its help sections.

### Dialogs
Confirmations are full-screen steps using the standard chrome, not bordered boxes. The DetailPortal is the only bordered overlay in the TUI.

### Detail Portal
A read-only scrollable overlay (`internal/tui/portal.go`) composited centered over the active view via the in-repo `compositeOverlay` helper:
- **Sizing:** 2-cell margin to every terminal edge; rounded accent (62) border; resizes with the terminal.
- **Chrome:** the bare portal title (no `sentei ─` — the brand is on the chrome behind the box), `┄` rule, `bubbles/viewport` body, `esc close · j/k scroll` hints, `↓ N%` indicator only while scrollable content remains below.
- **Keys:** `F1` toggles contextual help from any view; `?` toggles view-specific details where a view provides them (currently the worktree list). While open, scroll keys go to the portal, `esc` dismisses, `q`/`ctrl+c` always quit, and `F1`/`?` switch between help and details. All other keys are swallowed.
- **When to use:** progressive disclosure of read-only content that would clutter the main view (full branch lists, long error text, key references). Interactive content stays in regular views.

### Cleanup Flow (preview-first)
Destructive flows show their work before asking for it:
- **Preview-first:** entering cleanup runs a dry-run scan (spinner + `Scanning repository…` for at least the hold duration) and renders what each mode would do — `●` lines for actions, dim `·` lines for no-ops — before anything mutates.
- **Inline preview + portal:** aggressive candidates show the first 3 branch names inline with `and N more — ? for details`; the full list with dates and subjects lives in the detail portal.
- **Upgrade offer:** aggressive mode is offered contextually (`a aggressive` appears in the hints only when the scan found candidates) and double-confirms with the exact count: `Delete N branches? y/n`.

### Removal Safety Gate
Confirmation is friction reserved for risk:
- **Gate only at-risk selections.** A worktree is at-risk when it has uncommitted changes, untracked files, commits not on any remote, or a lock. Clean-and-pushed selections delete with no confirmation stop.
- **"Unpushed" requires a remote.** In a repo with no remotes the concept is meaningless and never gates.
- **At-risk indicators:** `[~]` uncommitted, `[!]` untracked, `[^]` unpushed, `[L]` locked — each with a WARNING count line in the gate.
- **CLI parity:** `--yes` skips prompts but never the gate; at-risk deletions need `--force` or interactive confirmation.
